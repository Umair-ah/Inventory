<p style="color: green"><%= notice %></p>

<h2 class="font-bold text-lg font-gray-800 mt-2">Un-Fulfilled Orders</h2>

<div class="flex flex-col w-full">
  <div class="overflow-x-auto sm:mx-0.5 lg:mx-0.5">
    <div class="py-2 inline-block min-w-full">
      <div class="overflow-x-auto">
        <table class="min-w-full overflow-x-scroll">
          <thead class="bg-white border-b">
            <tr>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Order ID
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Customer Email
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Address
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Total
              </th>
            </tr>
          </thead>

          <tbody>
            <% @not_fulfilled_orders.each do |o| %>
              <tr class="bg-gray-50 border-b">

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800 underline">
                  <%= link_to o.id, [:admin, o] %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  <%= o.customer_email %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  <%= o.address %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  $<%= o.total %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<h2 class="font-bold text-lg font-gray-800 mt-10">Fulfilled Orders</h2>

<div class="flex flex-col w-full">
  <div class="overflow-x-auto sm:mx-0.5 lg:mx-0.5">
    <div class="py-2 inline-block min-w-full">
      <div class="overflow-x-auto">
        <table class="min-w-full overflow-x-scroll">
          <thead class="bg-white border-b">
            <tr>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Order ID
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Customer Email
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Address
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left"> 
                Total
              </th>
            </tr>
          </thead>

          <tbody>
            <% @fulfilled_orders.each do |o| %>
              <tr class="bg-gray-50 border-b">

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800 underline">
                  <%= link_to o.id, [:admin, o] %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  <%= o.customer_email %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  <%= o.address %>
                </td>

                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                  $<%= o.total %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<%= link_to "New order", new_admin_order_path %>
